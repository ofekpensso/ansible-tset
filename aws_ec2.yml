plugin: amazon.aws.aws_ec2
regions:
  - us-east-1
filters:
  tag:Owner: ofekpenso
  instance-state-name: running

keyed_groups:
  - key: tags.Service
    prefix: service
  - key: tags.Name
    prefix: name

hostnames:
  - tag:Name

compose:
  ansible_host: public_ip_address
